import 'package:flutter/material.dart';

import '../../domain/entities/invoice_details_entity.dart';

class PaymentAddClientMailPage extends StatelessWidget {
  final List<EmailtoMystaffEntity> emailList;
  const PaymentAddClientMailPage({super.key, required this.emailList});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
